{
  "name": "todood-backend",
  "version": "1.0.0",
  "description": "Node.js + Express API server, includes database setup and migrations.",
  "main": "src/index.js",
  "scripts": {
    "docker:up:local": "docker compose --env-file .env.local up",
    "psql:local": "psql -U postgres -d todood -h localhost",
    "migrate:local": "psql -U postgres -d todood -h localhost -f",
    "dev": "NODE_ENV=development PORT=3000 nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Katie Soldau",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
